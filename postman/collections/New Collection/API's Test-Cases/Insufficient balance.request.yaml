$kind: http-request
name: Insufficient balance
description: |-
  Generated from cURL: curl -i -X POST \
  http://localhost:8080/api/v1/transactions/transfer \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
      "receiverWalletId": "VALID_RECEIVER_WALLET_ID",
      "amount": 10000.00,
      "referenceId": "TXN-INSUFFICIENT-001",
      "description": "Should fail"
  }'
url: http://localhost:8080/api/v1/transactions/transfer
method: POST
headers:
  Authorization: Bearer $TOKEN
  Content-Type: application/json
body:
  type: json
  content: |-
    {
        "receiverWalletId": "VALID_RECEIVER_WALLET_ID",
        "amount": 10000.00,
        "referenceId": "TXN-INSUFFICIENT-001",
        "description": "Should fail"
    }
order: 1787750015234
